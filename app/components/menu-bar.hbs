<nav aria-label={{t "nav.main"}} class="padding-side-large" ...attributes>
  <ul class="menu-bar-categories">
    {{#each this.thumbs as |thumb|}}
      <MenuBarItem
        @keyroute={{thumb.keyroute}}
        @name={{thumb.name}}
        @sprite={{thumb.sprite}}
      />
    {{/each}}
  </ul>
</nav>

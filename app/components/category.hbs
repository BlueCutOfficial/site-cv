<div class="category">
  {{!-- Header : category title --}}
  <div class="category-container alignleft">
    <img src={{@model.sprite}}
      alt="{{t "nav.category"}} {{t (concat "category." @model.id)}}"
    />
    <div class="category-line">
      <div class="title-container">
        <div class="category-title">{{t (concat "category." @model.id)}}</div>
      </div>
    </div>
  </div>

  <div class="container">
    <nav aria-label="{{t "nav.secondary"}} {{t (concat "category." @model.id)}}" class="row">
      {{!-- Show category' slots --}}
      <ul class="category-slots alignleft scrollable widthmax {{this.colClassSlots}}">
        {{#each @model.slots as |slot|}}
          <Slots::ProjectSlot
            @slotParams={{slot}}
            @slotRoute={{@model.slotRoute}}
          />
        {{/each}}
      </ul>

      {{!-- Show category badges --}}
      <div class="category-slots alignleft scrollable inline {{this.colClassBadge}}">
        {{#each @model.badges as |badge|}}
          <Slots::BadgeSlot @slotParams={{badge}} />
        {{/each}}
      </div>
    </nav>
  </div>
</div>
